const pointer debugout = 0xc000;
const pointer debugin = 0xc001;
const pointer true = 0x7d00;        // constant ff
const pointer false = 0x7d01;       // constant 0

macro output(src, dest)
{
    var int x;
    nfc(x, val(0xff));
    nfc(x, src);
    nfc(dest, x);
}



function int dostuff(pointer p)
{
    dostuff = 0xc5;
}

function main()
{
    var int x;
    x = dostuff(0x1234);
    output(x, debugout);
}

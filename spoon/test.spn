const pointer debugout = 0xc000;
const pointer debugin = 0xc001;

function main()
{
    var int x, y;
    nfc(y, y);
    nfc(y, y);
    if (y)
    {
        nfc(y, x);
    }
    else
    {
        var int z;
        nfc(z, y);
    }
    loopStart:
    nfc(x, 0x0018);
    nfc(x, debugin);
    nfc(debugout, x);
    goto loopStart;
}

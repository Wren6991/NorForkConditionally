#include <stddefs>

const pointer debugout = 0xc000;
const pointer debugin = 0xc001;

function main()
{
    var int x;
    nfc(x, 0x0018);
    nfc(x, debugin);
    nfc4(debugout, x, 0x0000, 0x0000);
}
